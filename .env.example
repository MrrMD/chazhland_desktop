# Скопируй в .env. По умолчанию (без .env) работает mock-режим.
VITE_API_BASE=http://localhost:8080
VITE_WS_URL=ws://localhost:8080/ws
# mock-режим: true — данные из src/mocks (без бэка); false — живой бэк
VITE_MOCK=true
